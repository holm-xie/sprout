#!/bin/bash

if [ ! -e /etc/clearwater/s-cscf.json ]
then

cat > /etc/clearwater/s-cscf.json << EOF
{
   "s-cscfs" : [
       {   "server" : "sip:${sprout_hostname}:5054;transport=TCP",
           "priority" : 0,
           "weight" : 100,
           "capabilities" : []
       }
   ]
}
EOF
fi
